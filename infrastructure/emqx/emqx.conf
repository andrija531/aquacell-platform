## EMQX 5.x configuration (HOCON).
##
## Decision #53: per-device credentials, never a shared username/password.
## Homeowners can read their own Shelly config, so one extracted shared
## credential would compromise the whole fleet.
##
## Environment variables prefixed EMQX_ override anything set here.

node {
  data_dir = "/opt/emqx/data"
}

## ---------------------------------------------------------------------------
## Authentication
##
## Configuring any authenticator makes EMQX reject clients it cannot
## authenticate. There is no separate allow_anonymous flag in 5.x — an empty
## `authentication` list is what allows anonymous connections, so this block
## must not be removed.
##
## Devices are provisioned into the built-in database at commissioning time.
## Swap `built_in_database` for `postgresql` once the device registry in
## Postgres is the source of truth.
## ---------------------------------------------------------------------------
authentication = [
  {
    mechanism = password_based
    backend = built_in_database
    user_id_type = username
    password_hash_algorithm {
      name = sha256
      salt_position = suffix
    }
  }
]

## ---------------------------------------------------------------------------
## Authorization
##
## no_match = deny means a topic not explicitly allowed in acl.conf is
## refused. Default-deny is the point; do not relax it.
## ---------------------------------------------------------------------------
authorization {
  no_match = deny
  deny_action = disconnect
  cache {
    enable = true
    max_size = 32
    ttl = 1m
  }
  sources = [
    {
      type = file
      enable = true
      path = "/opt/emqx/etc/acl.conf"
    }
  ]
}

## ---------------------------------------------------------------------------
## Listeners
## ---------------------------------------------------------------------------
listeners.tcp.default {
  bind = "0.0.0.0:1883"
  max_connections = 100000
  ## Plaintext. docker-compose.yml binds this to 127.0.0.1 only.
  ## Disable it entirely before any device connects over a real network.
  enable = true
}

listeners.ssl.default {
  bind = "0.0.0.0:8883"
  max_connections = 100000
  enable = true
  ssl_options {
    ## Defaults are the self-signed certs shipped in the image, which keep
    ## local development working with no setup. For anything a real device
    ## connects to, override these to a real certificate — the compose file
    ## reads MQTT_TLS_CERTFILE / MQTT_TLS_KEYFILE from .env and passes them in
    ## as EMQX_LISTENERS__SSL__DEFAULT__SSL_OPTIONS__* overrides.
    ##
    ## The certificate matters because the DEVICE validates the SERVER. A
    ## Shelly with ssl_ca="ca.pem" checks the broker against its built-in CA
    ## bundle, so a Let's Encrypt certificate for a real hostname works with
    ## no per-device setup. A self-signed certificate requires uploading your
    ## CA to every device via Shelly.PutUserCA and ssl_ca="user_ca.pem".
    cacertfile = "/opt/emqx/etc/certs/cacert.pem"
    certfile = "/opt/emqx/etc/certs/cert.pem"
    keyfile = "/opt/emqx/etc/certs/key.pem"
    versions = ["tlsv1.3", "tlsv1.2"]

    ## `verify` controls whether EMQX demands a certificate FROM THE CLIENT.
    ## verify_none does not weaken the encryption and does not stop the device
    ## validating the server — with a real certfile above, traffic on 8883 is
    ## encrypted and the device authenticates the broker. What is missing is
    ## the second direction: clients authenticate with username/password
    ## rather than a certificate.
    ##
    ## Decision #53 wants mTLS where the provisioning flow can carry it. The
    ## Shelly supports it (Shelly.PutTLSClientCert / PutTLSClientKey, then
    ## use_client_cert=true), so this is a real next step and not a
    ## theoretical one. Turning it on before per-device certs exist locks
    ## every device out, which is why it is not the default:
    ##     verify = verify_peer
    ##     fail_if_no_peer_cert = true
    verify = verify_none
    fail_if_no_peer_cert = false
  }
}

## Not used by this project. Leave off to reduce attack surface.
listeners.ws.default   { enable = false }
listeners.wss.default  { enable = false }
listeners.quic.default { enable = false }

## ---------------------------------------------------------------------------
## MQTT session behaviour
##
## Devices report on change plus a heartbeat (decision #35), so sessions are
## long-lived and mostly idle. keepalive is negotiated per client; the
## multiplier controls how long EMQX waits before declaring a client gone.
## ---------------------------------------------------------------------------
mqtt {
  max_packet_size = 1MB
  keepalive_multiplier = 1.5
  ## Retain the last will so the core engine learns about ungraceful drops.
  ## Fleet-wide correlated drops are a delivery risk (decision #44), so these
  ## events are recorded, not just logged.
  max_topic_levels = 8
}

## ---------------------------------------------------------------------------
## Dashboard. Password comes from EMQX_DASHBOARD__DEFAULT_PASSWORD.
## ---------------------------------------------------------------------------
dashboard {
  listeners.http {
    bind = 18083
  }
}

log {
  console {
    enable = true
    level = warning
  }
}
